We use libpng as a STATIC LIBRARY

Build Configurations are named:

Release
Debug
DebugInRelease

and correspond (roughly) to win32 - lib [Debug]


Note that these use C runtime:
  libpng.lib  uses Multithreaded DLL       C Runtime
  libpngd.lib uses Debug Multithreaded DLL C Runtime